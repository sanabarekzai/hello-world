# Running the project

This is a static HTML project. Start the Replit web workflow to serve
`index.html` on port 5000:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```

The `index.html` page is served at `/`. The original `myfirstweb` page remains
available directly at `/myfirstweb`.